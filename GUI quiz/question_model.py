class Question:
    """This block of code creates a Question class that can be used to create question objects, 
    each with a text and an answer attribute."""
    def __init__(self, text, answer):
        self.text = text
        self.answer = answer
